<%= form_tag "/restaurants/#{@restaurant.id}", :method => 'patch' do %>

  <div>
    <label for="name">Name:</label>
     <input size="80" name="name" id="name" value=" <%=@restaurant.name %>" required>
  </div>

  <div>
    <label for="address">Address:</label>
    <input size="25" name="address" id="address" value="<%= @restaurant.address %>">
  </div>

    <div>
    <label>Cuisine Type: </label>
      <% Cuisine.all.each do |cuisine| %>
      <%counter = 0 %>
        <% @restaurant.cuisines.each do|temp| %>
          <%if cuisine == temp %>
            <input type ="checkbox" name="cuisine_ids[]" value = "<%= cuisine.id%>" checked ><%= cuisine.name %> </>
            <% counter= counter +1 %>
          <%end%>
        <%end%>
        <%if counter == 0 %>
          <input type ="checkbox" name="cuisine_ids[]" value = "<%=cuisine.id %>" ><%= cuisine.name %></>
        <%end%>
        <% counter = 0 %>
      <%end%>
    </select>
  </div>

  <input type="submit" name="commit" value="Update Restaurant">

<% end %>





